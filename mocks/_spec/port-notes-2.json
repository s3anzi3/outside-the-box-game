{
 "11": "FAITHFUL TO THE MOCK\n- CURVE, STUCK_PCT (0.99), WIN_AT (0.997), FREEZE_MS (4300), RETRY_DELAY (3000) and the 600ms fade are copied verbatim from mocks/q11-loading-99.html.\n- curveFill() and statusFor() are byte-for-byte the mock's logic, including the ellipsis characters in \"Initializing…\", \"Verifying your answers…\", \"Calculating your percentile…\", \"Finalizing your certificate…\", \"Almost there…\" and \"Finishing up…\".\n- Heading \"Compiling your results\" + 1..3 cycling dots on a 400ms period; layout proportions match the mock's play-area percentages exactly (heading 10%, status 24%, bar 40% / 66% wide / 26 tall, percentage 53%, RETRY 71% at 150x52).\n- Handle: 14 wide, barH+16 tall, offset -8, two hairline grip lines, ink normally and t.seal (antique gold) once stuck or grabbed. Drag band reaches 30px past each end and 22px above/below the track, exactly as the mock.\n- Grabbing kills the auto",
 "14": "WHAT WAS BUILT\nLevel14.ts is fully replaced: the mirrored-text \"Backwards Day\" is gone, replaced by the approved q14-leave-the-room mock. Contents:\n- Prompt \"Candidates may not be observed while answering.\" (display bold 26) + sub \"Proceed when unobserved.\" (body 15, fgDim), at play-area fractions 0.12 / 0.213 (matching the mock's 8% / 19% CSS box tops once line-box centring is accounted for).\n- The proctor's eye: the mock's SVG redrawn on canvas 1:1 (same viewBox 160x100 path M10,50 Q80,-8 150,50 Q80,108 10,50; iris r24 accent + accentDeep stroke, pupil r11 ink, glint r3.5, brow path at 0.55 alpha). Box 190x120 at play-area top 27%, centred. Pupil tracks gc.mouseX/Y with the mock's exact math (k = min(1, 15/len), y scaled 0.7). Blink uses the mock's 5.2 s keyframe curve (lid rect x0 y8 w160 h84 scaled about its top edge, clipped to the almond).\n- PROCTOR STATUS: WATCHING readout at 63%,",
 "15": "WHAT IS FAITHFUL\n\n- Whole mock ported: four pencil pairs (green/yellow warm-ups, red flush left/right, blue flush top/bottom), freehand strokes grown STEP=5px at a time toward the cursor, click-to-draw AND press-and-drag, clicking mid-stroke abandons the line, bumps cost a heart and throw the stroke out, RESET clears lines but never restores the corner or hearts, and the invisible top-left-corner rip that lets lines leave the paper.\n- Numbers all carried over: SEAL_R 12, START_R 18, HIT_R 12, SEAL_BLOCK_R 22, STEP 5, corner grab radius 70, pull 62, tap thresholds 350ms/25px, desk limit = paper expanded 60/60/22/27. Geometry is fractions of the play area, so at 1280x860 the seals land within a pixel of the mock's (244,235)/(316,307)/(964,451)/(856,487)/(126,379)/(1154,379)/(640,185)/(640,538) and the corner on (115,122). Tear polygon is the mock's 15 points as offsets from the paper corne",
 "16": "WHAT SHIPPED\n- Level16.ts keeps `drawLevel16`, the puzzle and the win copy (\"CORRECT.\" / \"You listened to the one voice that was actually on your side.\", next level 17). The four cards now use the fountain-pen ink palette from the mock: RED #C03A2E, BLUE #2E6BA8, GREEN #3F8F55, YELLOW #D8A81F.\n- Text colour: the mock gives RED/BLUE/GREEN ivory (#F7F1E3 via .choice) but overrides YELLOW to ink (#1E1A15, `style=\"...; color:#1E1A15\"`, line 145). I followed the mock, not the brief's blanket \"ivory text\": ivory on #D8A81F is roughly 1.9:1 and unreadable. Say the word if you want all four ivory.\n- Layout percentages pulled to the mock's CSS: prompt top 12% (was 0.14), sub-line top 26% (was 0.28), card row top 46% (was 0.50); cards 0.182W x 0.226H with a 0.031W gap, which reproduces the mock's 190x86 / 32px gap at 1280x860 (centres 304.6 / 528.2 / 751.8 / 975.3, row centre y 389.6).\n- Engine co",
 "18": "WHAT WAS BUILT\nLevel18.ts is a full rewrite. The multiple-choice buttons and the \"( all values in this question are expressed in base 2 )\" fine print are gone. The play area now holds, top to bottom: the sum \"1  +  1  =  ?\" (bold display, 72*s, t.ink, at 0.27 of the play area), a 260x58*s centred drawTypeIn blank at 0.52, a 200x48*s \"SUBMIT  →\" drawButton at 0.74, and the mono form instruction \"WRITE YOUR ANSWER IN THE SPACE PROVIDED\" (10*s, t.fgDim, 1.4px tracking) at 0.925. Those fractions were derived from the mock's CSS tops (16% / 52% / 74% / 91%), with the sum's fraction shifted from the div top to a middle baseline.\n\nFAITHFUL TO THE MOCK\n- Only \"10\" wins: drawWinScreen(gc, 'TEN.', 'One plus one is 10. In the only language the grader speaks.', 19) — same title and body as M.win.\n- Hint ladder is the mock's, verbatim, in the mock's order, with the same fails counter incremented BEFO",
 "19": "WHAT WAS BUILT\nLevel19.ts is a full rewrite: the four answer buttons are gone, replaced by the mock's type-in blank. Layout is taken straight from the mock CSS as fractions of the play area (H 381 at 1280x860): mono caption \"COMPLETE THE SEQUENCE\" at 10%, the sequence O T T F F S S ? at 20.5% in bold display 64px with the trailing \"?\" in t.accent, the 200x58 blank at 54%, the 200x48 SUBMIT button at 76%, and the mono footnote \"WRITE YOUR ANSWER IN THE SPACE PROVIDED\" at 92%. Copy, ladder and numbers are the mock's, verbatim, and there are no em dashes in any player-facing string.\n\nLogic (faithful to q19.js): trim the value; an empty blank submits nothing and costs nothing; exactly \"E\" wins with drawWinScreen(gc, 'E.', 'One, Two, Three, Four, Five, Six, Seven. Eight. Capital E, as written.', 20). Anything else clears the blank, calls wrong(gc) (INCORRECT stamp, deny, one heart) and says i",
 "21": "WHAT SHIPPED\n\nLevel21.ts is a full rewrite against the engine contract, but the pong itself and the Wolfie2D machinery are preserved verbatim: Vec2 / StateMachine / State / GameEvent imports, FrodrickIdleState (drift to centre at |d|*6*dt capped at 4.8*dt), FrodrickChaseState (perfect per-frame tracking), and every source number from the mock (PLAYER_H .18, FRODRICK_H .46, PADDLE_W .018, BALL_R .013, PLAYER_LEFT .025, AI_RIGHT .975, SPEED_INIT .48, SPEED_MAX .95, PLAYER_SPEED 1.1, FRODRICK_SPEED 4.8, WIN_SCORE 3, speed growth *1.05, deflect *0.65, bounce startTime 0.45).\n\nEngine contract compliance:\n- No self-driven requestAnimationFrame any more. The old file scheduled its own rAF and called gc.render() from inside it; physics now advance from levelClock(gc, clock21) inside the single draw pass, so the level draws every frame and the main loop owns the clock.\n- Frozen while paused: step",
 "24": "SPEC SOURCE: mocks/_spec/parts/q24.js does NOT exist (the parts folder jumps q23 -> q25). I worked from the built mock mocks/q24-easy-one.html, whose inline script carries the full spec (DECOYS table, ANSWERS table, OPENING/DECOY_LINE/WIN_TITLE/WIN_BODY, live() gating), plus mocks/_test/tests/q24.test.js which asserts the mock's exact behaviour. Flagging this in case the orchestrator expected a parts file.\n\nWHAT LEVEL24.TS NOW DOES (drawLevel24 export name kept; screens/Level.ts unchanged):\n- Question \"15  +  15  =  ?\" at 30% of the play area, bold 68px*s displayFont, t.ink (was legacy t.fg), maxWidth topBoxWidth*0.9.\n- The same ten DECOYS at the same fractions and the same four ANSWERS (25 / 30 / 35 / 1515) at 17% width, 3% gaps, top at 50% of the play area, 56*s tall. Decoys drawChoice fontSize 13, answers fontSize 28, exactly as the previous build and the mock describe.\n- Every decoy:",
 "26": "WHAT WAS BUILT\nLevel26.ts is a full rewrite that keeps the level's identity (click the cookie 100 times) and adds every quirk from mocks/_spec/parts/q26.js:\n- Cookie drawn in the mock's own 200-unit design space (body radius 90, stroke 4, the eight chip circles at their exact viewBox coordinates), scaled to pixels by u = R/90 with R = min(topBoxWidth, topBoxHeight) * 0.189 (72px at 1280x860, matching the mock's 160px wrapper x 0.8 SVG scale).\n- Bites: every tenth click (up to 9 bites) clips a crescent out of the rim using an even-odd Path2D clip of the cookie box minus each BITES circle (r 30 design units), so the fill AND the crust stroke are cut exactly like the mock's SVG mask.\n- Crumbs: four per bite, positioned relative to the cookie (x within +/-1.25R, y 0.97R..1.16R below centre, rotation -30..+30 deg), which reproduces the mock's absolute play-area numbers (522+/-90 px, y 260..27",
 "27": "FAITHFUL TO THE MOCK\n- mocks/_spec/parts/q27.js does NOT exist (that folder has q18..q50 minus q20/q24/q27/q28). The built mock mocks/q27-keys-no-locks.html was the sole spec; I ported from its CSS/HTML/JS verbatim.\n- Copy ported exactly: directive \"ANSWER IN THE SPACE PROVIDED\"; riddle \"I have keys but no locks.\" / \"I have space but no rooms.\" / \"You can enter but cannot go inside.\" / \"What am I?\"; buttons HOUSE, MAP, PIANO, KEYBOARD in that order; SECRET = \"KEYBOARD\"; rolling buffer of the last 8 uppercased letters; Backspace erases one; keys ignored while paused/solved/gameOver.\n- Hint ladder ported exactly, including the mock's precedence order (the KEYBOARD line beats the second-miss line the first time KEYBOARD is clicked): first miss on any other button \"No. Keys, space, enter. It is closer than you think.\"; KEYBOARD click \"Yes. That is the word. That is not how you say it.\"; seco",
 "45": "WHAT WAS PORTED (faithful to mocks/_spec/parts/q45.js and the built mock)\n- Numbers exact: FLEE_RADIUS 150, FLEE_SPEED 9, button 150x52, STAMINA 2600 px, bolt freeze 260 ms, bolt slide 220 ms ease-out, bolt target picked from 40 candidates along the four walls with an early break at 380 px from the cursor, scream cooldown 900 ms, sheepdog line at 20 s. Walls: minX = 0.04W, maxX = 0.96W - BW, minY = 0.30H, maxY = 0.90H - BH; start at centre x, y = 0.45H. All lengths multiplied by uiScale(ctx) (s = 1 at 1280x860, so the game matches the mock pixel for pixel).\n- Copy exact: \"Press SUBMIT to continue.\" / \"...if you can. Some things run when you reach for them.\" / \"BUTTON STAMINA\" -> \"BUTTON WINDED\" / \"SUBMIT\" -> \"OK OK FINE\" / win \"CAUGHT.\" + \"You stopped chasing and started cornering. That is the whole trick.\" (next level 46).\n- Ladder exact: opening two lines (said in freshEntry so it is r",
 "50": "NO CHANGE NEEDED to levelData.ts: line 59 already reads exactly { title: \"Q50\", lines: [\"One final question, candidate. You have earned it.\", \"Think back to the very beginning.\"] }, which is the mock's closing M.retype split across two lines. The lines above are reported for completeness only.\n\nWHAT I ADDED (the two requested items)\n1. Examiner's last remark on the CORRECT screen. drawWin now calls say(gc, 'Certified. Corporate would like to remind you this is not a real certificate.') inside the existing one-shot winChimeFor guard, so state.guideLines carries it and the test can read it from g.state().remarks.\n2. STANDING AT COMPLETION on the certificate's serial line. New drawStandingLine() right-aligns \"STANDING AT COMPLETION <3 hearts>   ·   No. OTB-50-####\" at certX + certW - 28 on the Y(0.075) meta row. Hearts are renderer's drawHeart at 11*s, filled t.accent / t.accentDeep, spent "
}